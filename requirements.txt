# Pinned dependencies for the FYP pipeline.
# Versions reflect the tested local environment.
#
# Install:
#   pip install -r requirements.txt

# --- Phase 1 + Phase 2 + Phase 3 (data pipeline + features) ---
pandas==2.3.3
numpy==2.4.0
requests==2.32.5
httpx==0.28.1
fastapi==0.128.0
matplotlib==3.10.8
python-dotenv==1.2.1
openpyxl==3.1.5

# --- Phase 4 (ML model) ---
scikit-learn==1.8.0       # RandomForestRegressor, MultiOutputRegressor
joblib==1.5.3             # model persistence
shap==0.51.0              # explainability

# --- Phase 5 (FastAPI backend) ---
uvicorn==0.30.6           # ASGI server for FastAPI
